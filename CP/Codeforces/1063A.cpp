#include <bits/stdc++.h>

using namespace std;;

int t;
string s;

int main() {
    ios_base::sync_with_stdio(false);
    cin >> t >> s;
    sort(s.begin(), s.end());
    cout << s << "\n";
    return 0;
}
